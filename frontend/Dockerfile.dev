FROM node:20-alpine

WORKDIR /app

# Install dependencies
COPY package*.json ./
RUN npm ci

# Source files will be mounted as volumes
# No COPY of src needed for dev

EXPOSE 5173

CMD ["npm", "run", "dev"]
